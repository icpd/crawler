os: linux
language: go
env: GO111MODULE=on
go:
  - 1.13.x
script:
  - echo ${GITHUB_TOKEN}
#  - make releases

deploy:
  provider: releases
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  file: bin/*
  on:
    tags: true